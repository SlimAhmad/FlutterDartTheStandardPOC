name: hello_world
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  faker: ^2.1.0
  equatable: ^2.0.5
  uuid: ^4.3.3
  http: ^1.2.2
  shared_preferences: ^2.5.4
  http_exception: ^2.0.1
  flutter_dotenv: ^5.2.1

flutter:
  uses-material-design: true
  assets:
   - .env